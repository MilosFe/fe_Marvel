import React, { Component } from 'react';
import PropTypes from 'prop-types';

export const Header = props => {
    return (
        <header id="home">
            <img src="../marvel-portal-banner.jpg" />
        </header>
    );
};
